% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QuasarExperiment-class.R
\docType{class}
\name{QuasarExperiment-class}
\alias{QuasarExperiment-class}
\title{QuasarExperiment: a SummarizedExperiment for quasar eQTL mapping}
\description{
\code{QuasarExperiment} extends \link[SummarizedExperiment:RangedSummarizedExperiment]{SummarizedExperiment::RangedSummarizedExperiment}
to hold all data needed for the quasar eQTL mapping tool:
}
\details{
\itemize{
\item \strong{assays}: phenotype matrix (features × samples), e.g. mean or sum
pseudobulk counts
\item \strong{rowRanges}: genomic coordinates for each phenotype feature (gene)
\item \strong{colData}: per-sample metadata including covariates
\item \strong{geno}: lazy \link[BEDMatrix:BEDMatrix]{BEDMatrix::BEDMatrix} over the PLINK \code{.bed} file
(variants × samples); data are not loaded until subscripted
\item \strong{variantRanges}: \link[GenomicRanges:GRanges]{GenomicRanges::GRanges} for each variant, with
metadata columns \code{snp_id}, \code{ref}, \code{alt}
\item \strong{grm}: optional packed-symmetric genetic relatedness matrix
(\code{dspMatrix} from the Matrix package), or \code{NULL}
\item \strong{plinkPrefix}: path prefix for the PLINK file set, passed directly to
the quasar binary so the genotype data are never materialised in R
}
}
\section{Slots}{

\describe{
\item{\code{geno}}{A \link[BEDMatrix:BEDMatrix]{BEDMatrix::BEDMatrix} (variants × samples) or any
matrix-like object.}

\item{\code{variantRanges}}{A \link[GenomicRanges:GRanges]{GenomicRanges::GRanges} of length equal to
\code{nrow(geno)}.}

\item{\code{grm}}{A \code{dspMatrix} (Matrix package) of dimension samples × samples, or \code{NULL}.}

\item{\code{plinkPrefix}}{A length-one character string giving the PLINK file
prefix.}
}}

\seealso{
\code{\link[=QuasarExperiment]{QuasarExperiment()}} for the constructor, \code{\link[=runQuasar]{runQuasar()}} to invoke
the quasar binary.
}
