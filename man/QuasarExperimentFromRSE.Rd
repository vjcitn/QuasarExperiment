% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fromRSE.R
\name{QuasarExperimentFromRSE}
\alias{QuasarExperimentFromRSE}
\title{Construct a QuasarExperiment from a RangedSummarizedExperiment}
\usage{
QuasarExperimentFromRSE(
  se,
  plinkPrefix,
  covariateMatrix,
  grmFile = NULL,
  assayName = NULL,
  featureIdColumn = NULL
)
}
\arguments{
\item{se}{A \link[SummarizedExperiment:RangedSummarizedExperiment]{SummarizedExperiment::RangedSummarizedExperiment} whose
\code{rowRanges()} carry genomic coordinates for each feature and whose
\code{colnames()} match the individual IDs (IID) in the PLINK \code{.fam} file.}

\item{plinkPrefix}{Path prefix for the PLINK file set (\code{.bed}, \code{.bim},
\code{.fam}).}

\item{covariateMatrix}{A numeric matrix with rows corresponding to samples
(in the same order as \code{colnames(se)}) and columns corresponding to
covariates.  quasar requires all covariates to be numeric; categorical
variables must be dummy-coded before passing.  The recommended workflow is
to call \code{\link[stats:model.matrix]{stats::model.matrix()}} on \code{as.data.frame(colData(se))} — the
resulting intercept column (named \code{(Intercept)}) should be renamed \code{int}
to match quasar's convention.  The
\href{https://bioconductor.org/packages/ExploreModelMatrix}{ExploreModelMatrix}
package provides an interactive interface for building and inspecting
model matrices.}

\item{grmFile}{Optional path to a GRM TSV file (same format as accepted by
\code{\link[=QuasarExperiment]{QuasarExperiment()}}).  Pass \code{NULL} (default) to omit the GRM.}

\item{assayName}{Name of the assay in \code{se} to use as the phenotype matrix.
Defaults to the first assay.}

\item{featureIdColumn}{Name of a metadata column in \code{rowRanges(se)} to use
as the \code{phenotype_id}.  If \code{NULL} (default), \code{rownames(se)} are used.}
}
\value{
A \link{QuasarExperiment} object.
}
\description{
Converts an existing \link[SummarizedExperiment:RangedSummarizedExperiment]{SummarizedExperiment::RangedSummarizedExperiment} into
a \link{QuasarExperiment} by attaching PLINK genotype data and, optionally, a
genetic relatedness matrix.  This is the recommended entry point for users
who already hold their pseudobulk or bulk RNA-seq phenotype data in a
Bioconductor SE object (e.g. from \code{tximeta}, \code{muscat}, or \code{DESeq2}).
}
\details{
\strong{Sample matching}: \code{colnames(se)} are matched against the IID column of
the \code{.fam} file.  A warning is issued for any samples present in \code{se} but
absent from the \code{.fam}.

\strong{Coordinates}: \code{rowRanges(se)} must be a \code{GRanges} with valid \code{seqnames},
\code{start}, and \code{end}.  These are used directly as the feature coordinates;
no coordinate conversion is performed (Bioconductor uses 1-based closed
ranges throughout).
}
\examples{
exdir <- system.file("extdata", package = "QuasarExperiment")

# Build a minimal RangedSummarizedExperiment from the example data
qe_ref <- QuasarExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed"),
    covFile     = file.path(exdir, "cov-n100.tsv")
)
se <- as(qe_ref, "RangedSummarizedExperiment")

# Build a model matrix from colData and reconstruct
cd <- as.data.frame(colData(se))
mm <- model.matrix(~ sex + age + expr_pc1 + expr_pc2 +
                     geno_pc1 + geno_pc2 + geno_pc3 +
                     geno_pc4 + geno_pc5 + geno_pc6, data = cd)
colnames(mm)[colnames(mm) == "(Intercept)"] <- "int"
qe2 <- QuasarExperimentFromRSE(
    se              = se,
    plinkPrefix     = file.path(exdir, "chr22-n100"),
    covariateMatrix = mm
)
qe2

}
\seealso{
\code{\link[=QuasarExperiment]{QuasarExperiment()}} for construction from flat files,
\code{\link[=runQuasar]{runQuasar()}} to invoke the quasar binary.
}
