% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QuasarExperiment-constructor.R
\name{QuasarExperiment}
\alias{QuasarExperiment}
\title{Construct a QuasarExperiment from quasar input files}
\usage{
QuasarExperiment(
  plinkPrefix,
  phenoFile,
  covFile,
  grmFile = NULL,
  assayName = "pheno",
  genome = NA_character_
)
}
\arguments{
\item{plinkPrefix}{Path prefix for the PLINK file set (\code{.bed}, \code{.bim},
\code{.fam}). May be an absolute or relative path.}

\item{phenoFile}{Path to the phenotype BED file. The file is tab-separated
with columns \verb{#chr}, \code{start}, \code{end}, \code{phenotype_id}, then one column per
sample. Coordinates are 0-based half-open (BED convention).}

\item{covFile}{Path to the covariate TSV file. The file is tab-separated
with a header row; the first column (\code{sample_id}) identifies samples and
subsequent columns are covariates.}

\item{grmFile}{Optional path to the GRM TSV file. The file is
tab-separated: the first column is \code{sample_id} and remaining columns are
the same sample IDs in the header. Pass \code{NULL} (default) to omit the GRM.}

\item{assayName}{Name to assign the phenotype matrix in \code{assays()}.
Defaults to \code{"pheno"}.}

\item{genome}{Genome build string (e.g. \code{"hg38"}) assigned to the
\code{seqinfo} of \code{rowRanges}. Defaults to \code{NA} (unspecified).}
}
\value{
A \link{QuasarExperiment} object.
}
\description{
Reads phenotype, covariate, variant metadata, and (optionally) GRM files
into a \link{QuasarExperiment} object. Genotype data is represented lazily via
\link[BEDMatrix:BEDMatrix]{BEDMatrix::BEDMatrix}; the binary PLINK \code{.bed} file is not loaded into
memory.
}
\details{
\strong{Sample ordering}: samples are ordered by their appearance in the
phenotype file. Covariates and the GRM are reordered to match. The
\code{BEDMatrix} column order follows the \code{.fam} file; a mapping is stored in
\code{colData(qe)$fam_index} so that the correct genotype columns can be
extracted if needed.

\strong{BED coordinates}: the phenotype BED file uses 0-based half-open
coordinates. These are converted to 1-based closed ranges for
\code{rowRanges()}, consistent with Bioconductor conventions.
}
\examples{
exdir <- system.file("extdata", package = "QuasarExperiment")
qe <- QuasarExperiment(
    plinkPrefix = file.path(exdir, "chr22-n100"),
    phenoFile   = file.path(exdir, "mean-pheno-n100.bed"),
    covFile     = file.path(exdir, "cov-n100.tsv"),
    grmFile     = file.path(exdir, "grm-n100.tsv"),
    genome      = "hg38"
)
qe

}
